# Web Framework
Flask==3.0.0
Flask-CORS==4.0.0
python-dotenv==1.0.0

# Audio Transcription (Whisper)
transformers>=4.40.0
accelerate>=0.27.0
sentencepiece  # Required for some Whisper models
numpy

# Speaker Diarization (Pyannote)
torch>=2.0.0
torchaudio
pyannote.audio

# Audio Processing
soundfile  # For audio I/O
noisereduce  # For noise reduction preprocessing

# Audio Capture & Speech Detection (for live transcription testing)
pyaudio
webrtcvad
SpeechRecognition

# HTTP Client
requests

# Production Server
gunicorn==21.2.0
